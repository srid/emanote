# Defaults for Markdown frontmatter
order: 0
tags: []

templateTheme: blue

# Structured data stuff
siteMeta:
  siteName: Emabook
  siteTitle: Emabook
  description: "Emabook -- Markdown static site generator and live server. Supports Wiki-links, directories, HTML templates for complete theming."
  # You can specify raw HTML too.
  # Use as: <snippet:someHtml />
  someHtml: "<b>Ema</b> is <u>cool</u>!"
  # Or lists.
  # use as: <with:someList> ...
  someList: ["hello", "world"]