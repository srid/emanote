---
template:
  uptree:
    enable: true
---

# Focus Mode Fixture

This page renders every side region that note focus mode must hide: the
sidebar, right panel, and uptree.

## Right Panel Anchor

The heading gives the page a table of contents so the right panel renders in
the default layout.
